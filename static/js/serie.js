// Serie (single series detail) specific JS
document.addEventListener("DOMContentLoaded", () => {
    console.log("Serie detail page loaded");
    // TODO: bölüm tıklamaları, like/dislike vb. burada yönetilebilir.
});
